api:
  bind: 0.0.0.0:8080
  # Regex pattern that the email verification requires
  email_regex: (\d|\w){1,64}@((\d|\w){1,63}\.)?hs-heilbronn.de
  # This is required for authenticating before
  # creating a token
  username_password:
    admin: foobar

email:
  host: mail.example.de
  smtp_host: mail.example.de:587
  email: example@example.de
  # Most mail server don't require this
  # Leave empty if unsure
  identity: 
  username: admin
  password: foobar

database:
  host: 0.0.0.0:3306
  database_name: mail_verifier
  username: admin
  password: foobar
